apiVersion: v1
kind: Secret
metadata:
  name: {{ include "ai-inference-gateway.fullname" . }}-auth
  labels:
    {{- include "ai-inference-gateway.labels" . | nindent 4 }}
type: Opaque
stringData:
  DEMO_API_KEY: {{ .Values.auth.demoApiKey | quote }}
